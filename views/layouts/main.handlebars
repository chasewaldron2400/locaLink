<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>RootedBiz</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="flex-column justify-space-around">
    <header class="display-flex justify-space-between align-center p-2">
      <h1>
        <span role="img" aria-label="money">💸</span>
        <a href="/">RootedBiz</a>
      </h1>
      <nav>
        {{#if logged_in}}
        <a href="/profile">profile</a> |
        <a href="/categories">categories</a> | <!-- New link for browsing by categories -->
        <button class="no-button" id="logout">logout</button>
        {{else}}
        <a href="/login">login</a> |
        <a href="/categories">browse businesses</a> <!-- Access to browse without logging in -->
        {{/if}}
      </nav>
    </header>
    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
      <!-- Additional section for featured businesses or top-rated ones -->
      <section>
        <h2>Featured Businesses</h2>
        <!-- This would be dynamically filled with business data -->
      </section>
    </main>
    <footer>
      <p>&copy; {{year}} RootedBiz - All rights reserved.</p>
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
